[Unit]
Description=ProxAI - OpenAI-compatible API proxy
Documentation=https://github.com/lulkien/proxai
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=proxai
Group=proxai
WorkingDirectory=/var/lib/proxai
ExecStart=/usr/bin/proxai serve \
    --config /etc/proxai/config.toml \
    --key /var/lib/proxai/keys.db \
    --socket proxai
Restart=on-failure
RestartSec=5
NoNewPrivileges=yes
ProtectSystem=strict
ProtectHome=yes
ReadWritePaths=/var/lib/proxai
ReadOnlyPaths=/etc/proxai

[Install]
WantedBy=multi-user.target
